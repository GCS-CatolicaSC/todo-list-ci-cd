<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Minhas Tarefas</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <div class="container">
    <h1>✔ Minhas Tarefas</h1>
    <p class="subtitle">Organize seu dia de forma simples e eficiente</p>

    <form action="/tasks" method="POST" class="input-area">
      <input type="text" name="text" placeholder="Digite uma tarefa..." required>
      <button type="submit">+</button>
    </form>

    <div class="filters">
      <span><%= tasks.filter(t => !t.completed).length %> tarefa(s) pendente(s)</span>
      <div class="filter-buttons">
        <a href="/?filter=all">
          <button type="button" class="<%= filter === 'all' ? 'active' : '' %>">Todas</button>
        </a>
        <a href="/?filter=active">
          <button type="button" class="<%= filter === 'active' ? 'active' : '' %>">Ativas</button>
        </a>
        <a href="/?filter=completed">
          <button type="button" class="<%= filter === 'completed' ? 'active' : '' %>">Concluídas</button>
        </a>
      </div>
    </div>

    <div class="task-list">
      <% if (tasks.length === 0) { %>
        Nenhuma tarefa ainda. Comece adicionando uma!
      <% } else { %>
        <% tasks.forEach((task) => { %>
          <div class="task <%= task.completed ? 'completed' : '' %>">
            <span><%= task.text %></span>
            <div>
              <form action="/tasks/<%= task.id %>/toggle" method="POST" style="display:inline;">
                <button type="submit"><%= task.completed ? "Desmarcar" : "Concluir" %></button>
              </form>

              <form action="/tasks/<%= task.id %>/update" method="POST" style="display:inline;">
                <input type="hidden" name="text" value="<%= task.text %>">
                <button type="submit">Editar</button>
              </form>

              <form action="/tasks/<%= task.id %>/delete" method="POST" style="display:inline;">
                <button type="submit">Excluir</button>
              </form>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>
  </div>
</body>
</html>
